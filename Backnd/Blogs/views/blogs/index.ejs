<%- include("../partials/header") %> 

<head>
  <style type="text/css">
    .checked{
      color: orange;
    }
    .editbox{
      border: 0px !important;
    }
  </style>
</head>

 <div class="site-section">
      <div class="container">
        <div class="text-center">
          <h1 id="greet"></h1>
        </div>
          <hr>
          <div class="site-section">
      <div class="container blogs">
        <div class="row">
          <div class="col-md-12 col-lg-12">
            <div class="row">
              <h2 class="m-0">Our Blogs</h2>
              <form action="/blogs" method="GET" class="searchform ml-auto">
            <input type="text" name="search" placeholder="Search...">
            <button><i class="im im-magnifier"></i></button>
          </form>
          <% if(currentUser){ %>
           <!-- <a href="/blogs/new" class="btn btn-sm btn-secondary ml-2"><p class="m-0 mt-2">New POST</p></a> -->
        <% } %>
          
            </div>


            <% campgrounds.reverse().forEach(function(campground){ %>
            <div class="card mt-3 border-0 shadow" data-aos="zoom-out-up">
              <div class="row p-3 align-items-center">
              <div class="col-md-3">
                <a href="/blogs/<%= campground._id %>"><img src="<%= campground.image %>"></a>
              </div>
              <div class="col-md-9">
                <div class="row">
                  <div class="col-sm-6">
                    <a href="/blogs/<%= campground._id %>"><h3 class="text-black mb-0"><%= campground.name %></h3></a>
                  </div>
                  <div class="col-sm-6 text-right">
                    <a href="/blogs/<%= campground._id %>" class="btn btn-outline-secondary rounded openbtn"><p class="m-0">Open &rarr;</p></a>
                  </div>
                  <div class="container">
                  <% if (campground.rating === 0) { %>
                    <p><em class="text-muted">No reviews yet.</em></p>
                    <% } else { %>
                    <span class="fa fa-star checked"></span>
                    <span class="fa fa-star <% if (campground.rating > 1.5) { %> checked <% } %>"></span>
                    <span class="fa fa-star <% if (campground.rating > 2.5) { %> checked <% } %>"></span>
                    <span class="fa fa-star <% if (campground.rating > 3.5) { %> checked <% } %>"></span>
                    <span class="fa fa-star <% if (campground.rating > 4.5) { %> checked <% } %>"></span>
                    <% } %>
                  </div>
                </div>                
                <p><%= campground.outside %>
                </p>
              </div>
            </div>
            </div>
            <% }) %>

          </div>



          <!-- <div class="col-md-12 col-lg-3 text-center">
            <img src="images/logo1.png">
            <p>When your mind is so full of thoughts that each cross section in it seems to be blocked, let a pen become your brush and a piece of paper your canvas. This is when your words become you. It is easy to pen down your opinions and emotions in those pieces of paper rather than speaking them out loud. Express for us is just that piece of paper or our personal notes where we aim on pouring our heart out so that it would make it light and breezy. Moreover, We feel words lose their essence when spoken out but when penned on a piece of paper they have the power to reach millions of hearts. Express will focus on our fascination with words so that when you read you lure into your thoughts and are caught in a whirlwind of emotions.</p>

            <div class="row justify-content-around m-4">
              <i class="im im-facebook"></i>
              <i class="im im-instagram"></i>
              <i class="im im-snapchat"></i>
            </div>
          </div> -->

              </div>
            </div>
          </div>
        </div>
      </div>


     
<div class="container">
    <div class="row justify-content-center">
    <% if (pages && pages > 0) { %>
    <ul class="pagination text-center">
        <% if (current == 1) { %>
        <li class="disabled"><a>First</a></li>
        <% } else { %>
        <li><a href="/blogs<%if(search){%>?search=<%=search%><%}%>">First</a></li>
        <% } %>
 
        <% if (current == 1) { %>
        <li class="disabled"><a>«</a></li>
        <% } else { %>
        <li><a href="/blogs?page=<%= Number(current) - 1 %><%if(search){%>&search=<%=search%><%}%>">«</a></li>
        <% } %>
 
        <% var i = (Number(current) > 5 ? Number(current) - 4 : 1) %>
        <% if (i !== 1) { %>
        <li class="disabled"><a>...</a></li>
        <% } %>
        <% for (; i <= (Number(current) + 4) && i <= pages; i++) { %>
        <% if (i == current) { %>
        <li class="active"><a><%= i %></a></li>
        <% } else { %>
        <li><a href="/blogs?page=<%= i %><%if(search){%>&search=<%=search%><%}%>"><%= i %></a></li>
        <% } %>
        <% if (i == Number(current) + 4 && i < pages) { %>
        <li class="disabled"><a>...</a></li>
        <% } %>
        <% } %>
 
        <% if (current == pages) { %>
        <li class="disabled"><a>»</a></li>
        <% } else { %>
        <li><a href="/blogs?page=<%= Number(current) + 1 %><%if(search){%>&search=<%=search%><%}%>">»</a></li>
        <% } %>
 
        <% if (current == pages) { %>
        <li class="disabled"><a>Last</a></li>
        <% } else { %>
        <li><a href="/blogs?page=<%= pages %><%if(search){%>&search=<%=search%><%}%>">Last</a></li>
        <% } %>
    </ul>
    <% } %>
</div>
</div>
    
    
    </div>

 
 <%- include("../partials/footer") %>
